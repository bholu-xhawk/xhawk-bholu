{
  "name": "workspace",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "npm test --prefix apps/web",
    "build": "npm run build --prefix apps/web"
  },
  "workspaces": [
    "apps/web"
  ]
}
