{
  "name": "workspace",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "build": "echo 'No root build step'",
    "test": "pnpm -r test",
    "test:unit": "pnpm -r test",
    "test:e2e": "pnpm -C apps/web playwright test"
  }
}
