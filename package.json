{
  "name": "workspace",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "build": "npm --prefix apps/api run build",
    "test": "npm --prefix apps/api test"
  }
}

