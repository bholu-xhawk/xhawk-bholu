{
  "name": "workspace",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "build": "node -e \"console.log('No root build step')\"",
    "test": "npm run test:unit",
    "test:unit": "npm --prefix apps/web run test -- -i",
    "test:e2e": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.44.1"
  }
}

